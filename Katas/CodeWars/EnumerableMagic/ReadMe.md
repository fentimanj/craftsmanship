# EnumerableMagic

Create a function called 'one' that accepts two params: a sequence (eg [1,3,5,6,99,1,3]) and a function (eg bigger_than_ten) and then it returns 'true' only if the funciton in the params return true for exactly one (1) item in the sequence
